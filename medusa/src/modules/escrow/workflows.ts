/**
 * Escrow workflows (stub Sprint 10 Pha 2c v2 D23-EXPANDED Option C2)
 *
 * STATUS: 2 workflows commented out do escrow service drop.
 *
 * Original workflows:
 * - createEscrowWorkflow → svc.createEscrow (broken, dropped Bước 2)
 * - releaseMilestoneWorkflow → svc.releaseMilestone (broken, dropped Bước 2)
 *
 * Sprint 11+ TODO: Re-implement khi escrow service rewrite drives requirements.
 * Pattern reference: Pha 2a communication module workflows pattern.
 *
 * External consumers: 0 (verified grep audit Bước 4).
 */

export {}
