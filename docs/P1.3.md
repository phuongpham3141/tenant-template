# P1.3 Report — Caddy + hostname routing

**Date:** 2026-05-14
**Sprint:** 01 — Health Check & Gap Discovery
**Phase:** P1.3 — Caddy + hostname routing
**Duration:** ~15 phút (đã fix port binding trước phase P1.1)
**Verifier:** Claude Code

---

## Status

**PASS** — 9/9 hosts response 200/301/302/404 đúng kỳ vọng.

---

## URL pattern

**Option B** — Browser từ Windows gõ URL **không có port** (vì Caddy đã được rebind sang `0.0.0.0:80`).

> ⚠ Trước phase P1.1, file `compose-dev/docker-compose.yml` trên VM còn binding cũ `"8080:80"` → user phải gõ `:8080`. Đã sửa thành `"80:80"` + `docker compose up -d caddy` recreate. Container hiện listen `0.0.0.0:80->80/tcp, 0.0.0.0:443->443/tcp`.

---

## Output

### Curl 9 hosts từ VM (port 80)
```
http://shop.huayuesc.local/      -> 200   ← Storefront (vi default)
http://admin.huayuesc.local/     -> 301   ← Redirect / → /app (Medusa Admin)
http://cms.huayuesc.local/       -> 200   ← Payload root page
http://api.huayuesc.local/       -> 404   ← Expected: Medusa không serve / (chỉ /health, /store/*, /admin/*)
http://minio.huayuesc.local/     -> 200   ← MinIO Console
http://metrics.huayuesc.local/   -> 302   ← Redirect / → /login (Grafana)
http://en.huayuesc.local/        -> 200   ← Storefront EN locale
http://cn.huayuesc.local/        -> 200   ← Storefront CN locale
http://vi.huayuesc.local/        -> 200   ← Storefront VI explicit
```

### Caddyfile upstreams (parsed)
```
metrics.huayuesc.local  → grafana:3000
admin.huayuesc.local    → medusa-server:9000  (with / → /app redirect)
api.huayuesc.local      → medusa-server:9000
cms.huayuesc.local      → payload-cms:3000
minio.huayuesc.local    → minio:9001
shop/en/cn/vi.huayuesc.local → storefront:3001
:80 catch-all           → storefront:3001
```

Tất cả route khớp design.

---

## Browser test (Windows side, từ P1.1 verify)

Trước đó tôi đã test 8 URL bằng Invoke-WebRequest từ Windows host — `[P1.1.md](C:\install-medusa-dev\CMS\P1.1.md)` "Context" section. Tất cả response 200 + trang render bình thường.

---

## Caddy warnings (carry-over từ P1.1)

3 warning benign (không error):
1. Caddyfile format style không chuẩn → `caddy fmt --overwrite` 1 lệnh fix.
2. HTTP/2 skipped — đúng theo design `auto_https off`.
3. HTTP/3 skipped — same.

---

## Acceptance checklist

- [x] 9/9 hosts response 200/301/302/404 (không 5xx, không FAIL)
- [x] URL pattern xác nhận: **Option B** (no port — đã fix)
- [x] Browser test render OK (đã làm trong context P1.1)

---

## Findings (đã có trong gap-list từ P1.1)

| ID | Mô tả | Severity | Action |
|---|---|---|---|
| P1.1-pre | Compose binding `8080:80` cần commit fix (hiện chỉ sửa runtime trên VM, file Windows đã đúng nhưng VM repo `giaodien` branch chưa pull) | **P2** | Sync VM repo với branch `cms` trong Sprint 2 |
| P1.3-F1 | `vi.huayuesc.local` chưa có trong Windows hosts file (chỉ có 8 hosts hiện tại) | **P3** | Thêm dòng `192.168.0.11    vi.huayuesc.local` vào `C:\Windows\System32\drivers\etc\hosts` nếu cần test VI locale explicit |

---

## Next

Sang **P1.4 — Admin panels accessible** (~30 phút).
