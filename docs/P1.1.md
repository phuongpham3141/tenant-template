# P1.1 Report — Smoke test logs

**Date:** 2026-05-14
**Sprint:** 01 — Health Check & Gap Discovery
**Phase:** P1.1 — Smoke test logs (debug payload-cms restart)
**Duration:** ~15 phút
**Verifier:** Claude Code (medusa-dev session)

---

## Status

**PASS** · Classification: **A. Clean**

---

## Context trước khi chạy

Trước P1.1, đã phát hiện và fix một blocker không nằm trong roadmap:

- **Vấn đề:** 8 URL `*.huayuesc.local` không truy cập được từ Windows browser (port 80 fail, chỉ port 8080 mở)
- **Nguyên nhân:** `compose-dev/docker-compose.yml` trên VM bind Caddy `"8080:80"` (port 80 host không expose)
- **Fix:** `sed` đổi binding thành `"80:80"` + `docker compose up -d caddy` recreate
- **Kết quả:** 8/8 URL response 200 (admin/cms/api/shop/en/cn/minio/metrics)

Credentials verify: `phuongpham3141@gmail.com` / `Phuong2606$$` login Medusa API trả về JWT HTTP 200. File `tenant-template-credentials.html` khớp 100% với `.env` trên VM.

---

## Output (6 blocks log)

### Block 1 — payload-cms ERRORS
```
(empty — không có error/fatal/warn/exit pattern nào)
```

### Block 2 — payload-cms RAW LAST 30 LINES
```
GET /api/metrics 404 in 37ms      (lặp lại ~20 lần — Prometheus scrape)
GET /api/metrics 404 in 32ms
...
○ Compiling / ...
✓ Compiled / in 4.2s (1430 modules)
GET / 200 in 5019ms
○ Compiling /admin/[[...segments]] ...
✓ Compiled /admin/[[...segments]] in 28.7s (3764 modules)
GET /admin 200 in 254ms
```

### Block 3 — medusa-server errors
```
(empty)
```

### Block 4 — storefront errors
```
(empty)
```

### Block 5 — caddy errors/warnings
```
1. {"level":"warn","msg":"Caddyfile input is not formatted; run 'caddy fmt --overwrite' to fix inconsistencies"}
2. {"level":"warn","msg":"HTTP/2 skipped because it requires TLS","addr":":80"}
3. {"level":"warn","msg":"HTTP/3 skipped because it requires TLS","addr":":80"}
```

Cả 3 đều là warn benign — không phải error.

### Block 6 — Docker stats snapshot

| Container | CPU% | MEM% | MEM Usage |
|---|---|---|---|
| payload-cms | 154.88%* | 8.98% | 2.81 GiB / 31.34 GiB |
| storefront | 0.01% | 7.87% | 2.47 GiB / 31.34 GiB |
| medusa-server | 0.14% | 1.62% | 518 MiB / 31.34 GiB |
| grafana | 0.84% | 1.08% | 345 MiB / 31.34 GiB |
| prometheus | 0.00% | 0.39% | 124 MiB / 31.34 GiB |
| minio | 0.05% | 0.46% | 149 MiB / 31.34 GiB |
| postgres | 0.00% | 0.26% | 82 MiB / 31.34 GiB |
| meilisearch | 0.28% | 0.10% | 31 MiB / 31.34 GiB |
| redis | 0.78% | 0.05% | 17 MiB / 31.34 GiB |
| caddy | 0.00% | 0.05% | 15 MiB / 31.34 GiB |

\* payload-cms CPU spike vì đang compile `/admin/[[...segments]]` (3764 modules) chính lúc snapshot. Bình thường Next.js dev mode.

**Tổng memory dùng:** ~6.7 GiB / 31.34 GiB (21%) — dư rất nhiều.

---

## Diagnosis

Stack **clean, không có crash loop, không có OOM**. Phân loại **A** theo bảng trong roadmap P1.1:

- ❌ Không gặp B (repeating error)
- ❌ Không gặp C (OOM — MEM% cao nhất 8.98%)
- ✅ Khớp A (log empty hoặc warn nhỏ)

Payload-cms restart 27 phút trước (theo `docker compose ps` ban đầu) gần như chắc chắn là **manual rebuild** trong session trước (không có log error trước restart, container hiện không restart loop, memory ổn định).

---

## Findings phụ (ghi vào gap-list P1.7)

| ID | Mô tả | Severity | Action |
|---|---|---|---|
| P1.1-F1 | `payload-cms /api/metrics 404` lặp lại liên tục — Prometheus scrape endpoint chưa expose | **P3** | Thêm metrics endpoint hoặc remove scrape target trong `observability/prometheus/prometheus.yml` |
| P1.1-F2 | `Caddyfile.dev` format không chuẩn (warn 1 dòng) | **P3** | Chạy `caddy fmt --overwrite /etc/caddy/Caddyfile` 1 lần |
| P1.1-F3 | HTTP/2 và HTTP/3 disabled (do dev không TLS) | **P3 / không fix** | Đúng theo design `auto_https off`. Sang Sprint 7 staging có TLS sẽ tự bật |
| P1.1-pre | Compose binding `8080:80` ngược với credentials doc (đã fix) | **P2** | Cần commit fix vào branch để VM khi rebuild không revert |

---

## Acceptance checklist (theo roadmap)

- [x] Đã chạy 6 block lệnh, có output
- [x] Phân loại được vào A / B / C / Khác → **A**
- [x] Nếu B hoặc C: ghi chú error pattern — N/A vì là A

---

## Next

Sang **P1.2 — Database verification** (read-only SQL, ~20 phút).

Đợi user confirm `tiếp` / `next` trước khi chạy P1.2 theo roadmap.
