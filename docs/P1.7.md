# P1.7 Report — SPRINT 1 CLOSED

**Date:** 2026-05-14
**Sprint:** 01 — Health Check & Gap Discovery
**Phase:** P1.7 — Sprint 1 wrap-up
**Status:** **CLOSED**
**Output:** [sprint-01-gap-list.md](sprint-01-gap-list.md)

---

## TL;DR

Sprint 1 hoàn tất. **22 findings categorized:**
- 🔴 **3 P0 blocker** (1 đã fix, 2 cần Sprint 2 priority 1)
- 🟡 **5 P1 critical**
- 🟢 **5 P2 important**
- ⚪ **8 P3 nice-to-have**

**Buyer flow** đi tới bước 5/8 (browse — đạt mục tiêu roadmap "ít nhất bước 5"). Gãy ở signup do storefront thiếu `/api/auth/*` routes.

**Stack healthy**: 10/10 containers Up >41h, không OOM, không crash loop. Schema 618 tables / 1658 indexes / 293 RLS / 43 migrations success.

---

## Auto-fixes applied

| # | Fix | Phase | Justification |
|---|---|---|---|
| 1 | Caddy port `8080:80` → `80:80` (recreate container) | P1.3 | URL accessibility blocker, runtime-only — file Windows đã đúng |
| 2 | Created Payload first admin (`phuongpham3141@gmail.com`) | P1.4 | Login fail 401 blocker. First-register endpoint là non-destructive |

Cả 2 fix **không touch git history** — chỉ runtime state trên VM.

---

## What broke vs design

### Schema layer ✅
Foundation rất chắc. 482 domain tables + 136 Medusa core, 1658 indexes, 293 RLS tables — vượt expected. 43 migrations all success.

### Middleware layer 🔴
Plan R19 nói storefront middleware redirect unauth tới /login. Actual middleware chỉ set locale cookie → toàn bộ protected pages public access. **Mất tầng phòng vệ chính**.

### Storefront /api layer 🔴
Plan R19 + R20 nói có ~12 SDK clients, 8 server actions, 5 auth routes. Actual: 0 routes trong `/app/api/*`. **UI shell + sạch backend wiring**.

### Cấu hình env 🟡
- `NEXT_PUBLIC_MEDUSA_PUBLISHABLE_KEY` thiếu → Storefront không gọi Medusa store API.
- `NEXT_PUBLIC_MEDUSA_BACKEND_URL` có nhưng vô dụng nếu thiếu key.

### Extensions 🟡
`pgvector` + `pg_partman` chưa cài — postgres:16.4-alpine image stripped. R21 Visual Search HNSW + R22 partition rotation đều disabled.

### Seed data 🟡
Suppliers 7/7 ✅. RFQ 2/2 ✅. Quotes 3/3 ✅. Products 4/25 (chỉ Medusa default) — 21 missing.

### DB Findings (R23 validation) 🔴
- 8 tenant tables RLS_ENABLED_BUT_NO_POLICY — silent block. **P0 phải fix trước khi wire tenant module.**
- 416 FK columns thiếu index — performance.

---

## Gap list summary

Đầy đủ trong [sprint-01-gap-list.md](sprint-01-gap-list.md). Top 5 critical:

| Rank | ID | Mô tả | Severity | ETA |
|---|---|---|---|---|
| 1 | P1.6-F1 | 8 tenant tables RLS no-policy | P0 | 5 phút |
| 2 | P1.5-F1 | Middleware không check auth | P0 | 4h |
| 3 | P1.5-F2 | Storefront thiếu /api/* routes | P0 | 22h |
| 4 | P1.6-F2 | 416 FK indexes missing | P1 | 30 phút |
| 5 | P1.2-F1 | pgvector chưa cài | P1 | 30 phút |

---

## Sprint 2 estimate

**Week 1 — Hardening:** ~10h (DB + auth quick fixes)
**Week 2 — Storefront wiring:** ~22h (signup/cart/RFQ routes)
**Total:** ~32h ≈ **5 ngày dev** (gọi rounded là 1 sprint week).

---

## Acceptance checklist

- [x] File `docs/sprint-01-gap-list.md` đã save (tại `C:\install-medusa-dev\CMS\sprint-01-gap-list.md`)
- [x] Có entry trong section "✅ Verified working" (>20 entry)
- [x] P0/P1/P2/P3 categorized (3 + 5 + 5 + 8 = 21 + 1 audit-fixed)
- [⏳] Commit + push to `cms` branch (TODO — đợi user xác nhận)

---

## Sprint 2 readiness

**Ready to start** với câu hỏi mở cần user clarify:

1. **Branch strategy:** VM `giaodien` cũ vs Windows `cms` — sync force, hay giữ VM dirty?
2. **Migration `43_fk_indexes.sql` strategy:** 1 file vs split theo schema?
3. **Priority order:** Buyer flow trước hay DB hardening trước?
4. **Default Medusa products:** Xóa hay giữ baseline?

---

## File outputs Sprint 1 (đầy đủ tại `C:\install-medusa-dev\CMS\`)

```
P1.1.md   — Smoke test logs
P1.2.md   — Database verification
P1.3.md   — Caddy + hostname routing
P1.4.md   — Admin panels accessible
P1.5.md   — Storefront end-to-end
P1.6.md   — R23 validation suite
P1.7.md   — Sprint 1 wrap-up (this file)
sprint-01-gap-list.md   — ★ Output cuối Sprint 1
```

Validation raw on VM: `/tmp/validation-20260515-032244.txt` (174 KB).

---

## Next action

Đợi user gõ:
- `commit` → tôi sẽ commit + push gap-list lên `origin/cms`
- `sprint2` → tôi sẽ bắt đầu Sprint 2 với priority queue đề xuất (DB hardening trước, storefront sau)
- `priority <fix-id>` → cherry-pick fix cụ thể trước (vd: `priority P1.6-F1` để fix tenant RLS ngay)
- Bất kỳ câu hỏi nào về gap-list trước khi quyết định Sprint 2 path
